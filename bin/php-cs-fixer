#!/usr/bin/env bash

set -eu

. get_tty
. get_user

docker run \
    -e LOCAL_USER_ID=$DOCKER_HOST_USER_ID \
    -i${TTY} \
    --rm \
    -v $(pwd):/workdir:delegated \
    gcr.io/docker-1000101/php-cs-fixer:2.16.1-e789817 "$@"
